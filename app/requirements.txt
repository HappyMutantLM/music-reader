fastapi
uvicorn[standard]
pymupdf
# pillow, aiofiles: still unused by the backend — routers/pages.py
# renders straight from PyMuPDF (fitz.csGRAY) with no PIL step, and the
# page-image cache (CACHE_DIR) is plain synchronous file I/O. Kept in
# case a thumbnail feature or async file serving comes up later; drop
# both if that never materializes.
pillow
aiofiles
# sqlite-utils: currently unused — database.py talks to sqlite3
# directly. No known future-use story like pillow/aiofiles above; flag
# for removal if nothing ends up needing it.
sqlite-utils
python-multipart
watchdog