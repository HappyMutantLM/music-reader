fastapi
uvicorn[standard]
pymupdf
# pillow: used by routers/pages.py to crop each rendered page to its
# actual printed content before fitting it to the panel (see
# CONTENT_WHITE_THRESHOLD/_render_page_png). Do not remove.
# aiofiles: still unused — the page-image cache (CACHE_DIR) is plain
# synchronous file I/O. Kept in case async file serving comes up later;
# drop if that never materializes.
pillow
aiofiles
# sqlite-utils: currently unused — database.py talks to sqlite3
# directly. No known future-use story like pillow/aiofiles above; flag
# for removal if nothing ends up needing it.
sqlite-utils
python-multipart
watchdog